---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import "@/styles/global.css";
---

<Layout
  title="Inicio"
  description="Informacion general de Mundo Electronico, tu tienda de componentes electronicos."
>
  <Header slot="header" />

  <section class="hero">
    <div class="container">
      <h1 class="hero__title">
        Todo para tus proyectos <span class="hero__accent">Electrónicos</span>
      </h1>

      <p class="hero__subtitle">
        Componentes electrónicos originales y listos para entrega en Ibagué.
        Compra fácil, rápida y segura. Recoge en tienda o solicita domicilio.
      </p>
    </div>
  </section>

  <section class="info">
    <div class="container grid">
      <article class="card">
        <h2>Sobre nosotros</h2>
        <p>
          Somos una tienda especializada en componentes electrónicos ubicada en
          Ibagué, Calle 17 # 4 - 32. Atendemos estudiantes, técnicos y
          desarrolladores que buscan calidad, disponibilidad y asesoría confiable.
        </p>
      </article>

      <article class="card">
        <h2>Cómo comprar</h2>
        <p>
          Explora el catálogo, agrega los productos al carrito y finaliza tu pedido
          en minutos. Puedes recoger en nuestra tienda física o solicitar envío a
          domicilio. Los tiempos y costos varían según la distancia.
        </p>
      </article>

      <article class="card">
        <h2>Por qué elegirnos</h2>
        <p>
          Precios claros y productos seleccionados para proyectos
          reales de prototipado, reparación y aprendizaje técnico. Compra con
          confianza.
        </p>
      </article>
    </div>
  </section>

  <section class="cta">
    <div class="container cta__box">
      <h2>Empieza hoy tu próximo proyecto</h2>
      <a href="/catalog" class="btn btn-primary">Ver catálogo completo</a>
    </div>
  </section>

  <Footer slot="footer" />
</Layout>

<style>
  .hero {
    padding: var(--space-xl) 0 var(--space-lg);
    text-align: center;
  }

  .hero__title {
    font-size: clamp(1.8rem, 5vw, 2.8rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.1;
  }

  .hero__accent {
    background: linear-gradient(
      135deg,
      var(--color-primary),
      var(--color-accent)
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero__subtitle {
    margin-top: var(--space-sm);
    color: var(--color-text-muted);
    font-size: 1.05rem;
    max-width: 480px;
    margin-inline: auto;
  }

  .hero__actions {
    margin-top: var(--space-lg);
    display: flex;
    justify-content: center;
    gap: var(--space-sm);
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 10px 18px;
    border-radius: var(--radius-md);
    text-decoration: none;
    font-weight: 600;
    border: 1px solid transparent;
    transition: all var(--transition-fast);
  }

  .btn-primary {
    background: var(--color-primary);
    color: #fff;
  }

  .btn-primary:hover {
    filter: brightness(1.08);
  }

  .btn-secondary {
    border-color: var(--color-border);
    color: var(--color-text);
    background: var(--color-surface);
  }

  .btn-secondary:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  .info {
    padding: var(--space-xl) 0;
  }

  .grid {
    display: grid;
    gap: var(--space-md);
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  }

  .card {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-surface);
    padding: var(--space-lg);
  }

  .card h2 {
    margin-bottom: var(--space-sm);
    font-size: 1.1rem;
  }

  .card p {
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  .cta {
    padding: 0 0 var(--space-2xl);
  }

  .cta__box {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
    flex-wrap: wrap;
    background: linear-gradient(
      120deg,
      rgba(56, 189, 248, 0.12),
      rgba(56, 189, 248, 0.02)
    );
  }

  .cta__box h2 {
    font-size: clamp(1.2rem, 3vw, 1.7rem);
    margin: 0;
  }
</style>
