// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://jdgiraldot.github.io',
    base: 'front-mundo-electronico',
    output: 'static',
    devToolbar: { enabled: false },
});
